# ── Dexvra Telegram Bot — copy to .env (gitignored; NEVER commit real values) ──

# ── Telegram ─────────────────────────────────────────────────────────────────
BOT_TOKEN=                       # @BotFather token for the main bot
ADMIN_BOT_TOKEN=                 # @BotFather token for the admin/template-editor bot (optional)
# The main bot's @handle. It is NOT read from BOT_TOKEN — nothing calls getMe —
# so it is used verbatim to build the "➕ Add to your group" link and the {bot}
# mention in every template and channel post. Correct as-is for the Dexvra
# operator; anyone running their own instance MUST change it, or every one of
# their users is sent to @dexvrabot.
BOT_USERNAME=dexvrabot
# The @handle of the trading bot the "⚡ Trade" buttons in buy alerts and channel
# posts point at. THIS process does not run it — it is a separate bot with its
# own token and its own process, living in ../tradebot (own package.json, own
# .env). This value is only the link target.
TRADEBOT_USERNAME=dexvratradebot
# The three channels — Dexvra's own, and correct as-is for its operator (the bot
# must be admin in each). On any other instance, change them: a bot that is not
# admin there fails to post with a single WARN, which goes nowhere unless
# LOG_CHANNEL is set — so a purchase succeeds and nothing appears anywhere.
ANNOUNCE_CHANNEL=@dexvraio        # announcement channel (bot must be admin)
TRENDING_CHANNEL=@dexvratrending  # trending channel
LISTING_CHANNEL=@dexvralisting    # listing-alert channel
# Community group. Every listing is FORWARDED here from LISTING_CHANNEL, so the
# post keeps its premium emoji and carries a "from Dexvra Listing Alerts" header.
# The bot must be a member of the group. Set it EMPTY to turn the mirror off.
GROUP_CHAT=@dexvragroup
LOG_CHANNEL=                      # optional: channel id for visitor + event logs (/start, purchases)
# Optional second channel for warnings and errors. Leave blank and they land in
# LOG_CHANNEL alongside the visitor/purchase feed; set it and that feed stays clean.
ERROR_CHANNEL=
# Hour (UTC) for the daily 💚 health heartbeat to ERROR_CHANNEL. Its ABSENCE is
# the alert: an in-process monitor cannot report its own death, so a missing
# line means the bot is down. Default 1 = 08:00 WIB.
HEALTH_HEARTBEAT_HOUR_UTC=1
PK_CHANNEL=                       # optional: PRIVATE channel id for temp-wallet private-key backups (bot must be admin)
ADMIN_IDS=                        # comma-separated numeric Telegram user ids (these pay 0 / test free)

# ── Token logo → animated custom-emoji packs (optional) ──────────────────────
# Each listing's logo becomes an animated custom emoji in a per-token pack
# ("SYMBOL by @dexvralisting"), shown inline in channel posts via GramJS.
# Packs are owned by the ADMIN bot; needs an anchor user that has /start'ed it.
# TOKEN_PACK_BOT_TOKEN=           # defaults to ADMIN_BOT_TOKEN
# TOKEN_PACK_USER_ID=             # defaults to the first ADMIN_IDS entry
# TOKEN_PACK_TITLE_SUFFIX=        # defaults to " by @dexvralisting"
ADMIN_USERNAMES=                  # comma-separated @usernames (case-insensitive)

# ── Premium emoji (GramJS / MTProto) ─────────────────────────────────────────
# Channel posts are sent from a Telegram PREMIUM user account so premium custom
# emoji render animated. 1) create API creds at https://my.telegram.org/apps
# (log in with the premium account), 2) run `node scripts/gramjs-login.js` once
# on the server, 3) make the account able to post in all three channels.
# Without these the bot falls back to Bot API (plain unicode emoji) — no outage.
API_ID=                           # from my.telegram.org/apps
API_HASH=                         # from my.telegram.org/apps
GRAMJS_SESSION_FILE=              # default: <bot>/session.txt (created by the login script)
GRAMJS_ENABLED=1                  # set 0 to force Bot API posting
# (PREMIUM_EMOJI was removed: the built-in premium IDs were a third-party
# fourtis pack and rendered fourtis branding. Defaults are plain unicode;
# paste your OWN premium emoji into templates via @dexvraadminbot instead.)

# ── Dexvra web app (internal API) ────────────────────────────────────────────
SITE_URL=https://dexvra.io
DEXVRA_API_BASE=http://127.0.0.1:3005   # where the Next.js app listens (same VPS)
INTERNAL_API_TOKEN=               # MUST equal the web app's INTERNAL_API_TOKEN (>= 24 chars)

# ── Payment (temp wallet + poll) ─────────────────────────────────────────────
PAYMENT_POLL_MS=3000              # balance poll interval (clamped 1500–10000)
PAYMENT_TIMEOUT_MS=300000         # give up polling after this (5 min)
PAYMENT_TOLERANCE_PCT=0           # accept payments this % under quote (dust tolerance)

# ── RPC endpoints ────────────────────────────────────────────────────────────
# Every chain ships with a LIST of keyless public endpoints (src/config/rpc.js).
# A READ walks the list until one answers; a SEND never falls back (one sweep
# stays on whichever node answered its opening balance read, for every attempt).
# So every url listed here is a node a sweep MAY BROADCAST FROM — list only ones
# you trust to send. Two ways to override:
#
#   RPC_<CHAIN>=url             leads the list, the public defaults stay as backup
#   RPC_<CHAIN>_URLS=a,b,c      REPLACES the list entirely (your nodes only)
#
# The defaults are shared with the whole internet and rate-limit hard. If your
# groups are busy, or you take real payments, put a paid endpoint first — that
# is the single highest-value line in this file after BOT_TOKEN.
#
#   npm run rpc:check           which endpoints actually answer from this server
#
# Chains with a wallet adapter (payments + sweeps happen here):
RPC_ETHEREUM=
RPC_BSC=
RPC_BASE=
RPC_ROBINHOOD=
RPC_SOLANA=
RPC_TRON=
RPC_TON=
# Tracked-only EVM chains — billed on another chain, so no wallet is created
# here. They still need an endpoint: the buy bot reads holder balances on them,
# and without one whale detection does not fail, it just never finds a whale.
# All have public defaults; set these only to override.
RPC_POLYGON=
RPC_ARBITRUM=
RPC_OPTIMISM=
RPC_AVALANCHE=
RPC_BERACHAIN=
RPC_SONIC=
RPC_HYPEREVM=
RPC_ABSTRACT=
RPC_APECHAIN=
RPC_BLAST=
RPC_SEI=
RPC_UNICHAIN=
RPC_PLASMA=
TON_API_KEY=                      # toncenter.com API key (recommended for TON)

# ── Treasury (sweep destinations) ────────────────────────────────────────────
# After a payment clears, funds sweep to these. Leave a chain BLANK to skip the
# sweep for it (funds stay in the per-order temp wallet, whose key is stored in
# .keys/). Set these before going live.
TREASURY_EVM=                     # one address for ethereum / bsc / base / robinhood
TREASURY_SOL=
TREASURY_TRON=
TREASURY_TON=

# ── Temp-wallet key storage ──────────────────────────────────────────────────
# WALLETS_DIR=./.keys
WALLET_ENC_KEY=                   # 64-hex (32-byte) AES-256-GCM key to encrypt stored keys at rest

# ── MongoDB durable mirror (optional; recommended for production) ─────────────
# When set, all bot state (the /start audience, orders, templates, group +
# banner config, dedup latches) is mirrored to MongoDB so it survives a VPS
# reset / container replace instead of living only on local disk. Reads still
# use local files; at boot any store missing on disk is restored from Mongo.
# Leave BLANK to run on local files only (no outage). Point BOTH bots (main +
# admin) at the SAME MONGO_URI so they share one durable store.
#
# THE STORES THAT ACTUALLY HURT TO LOSE are templates.json — every message an
# admin reworded, INCLUDING the premium emoji they pasted into it — and
# tokenemoji.json, the custom_emoji_id of the animated pack minted per paid
# listing. The packs themselves live on Telegram and survive this VPS; the
# mapping from id to token and to template position does not. Verify with
# `npm run backup:check`, which exits non-zero if anything is unmirrored.
MONGO_URI=                        # e.g. mongodb+srv://user:pass@cluster.mongodb.net/dexvra
# MONGO_DB=dexvra                 # optional; defaults to the db name in MONGO_URI
# Each save mirrors fire-and-forget so it can never delay a payment or a send —
# which also means a failed mirror write is silent. This is how often the bot
# re-checks that every store really is in Mongo and retries the ones that are
# not. 0 disables the sweep (boot-time convergence still runs).
# MIRROR_SWEEP_MS=300000

# ── Twitter / X — AUTO-POSTING ───────────────────────────────────────────────
# THE RULE: only LISTINGS (paid + free auto), PUMP ALERTS and BANNER ADS are
# posted to the account below. Trending Token, the Top Gainers board and rank-up
# alerts are NOT (see the switches further down). Leave the four keys blank and
# the whole X path stays OFF — the bot runs normally, it just never tweets, and
# it says so once at boot.
#
# ── THE 4 KEYS THE BOT NEEDS (all four, from the SAME app) ────────────────────
# console.x.com → your project → your app (e.g. "DexvraListing") → Keys and tokens
#
#   X_API_KEY         = OAuth 1.0a  "Consumer Key"          (shown as API Key)
#   X_API_KEY_SECRET  = OAuth 1.0a  "Consumer Secret"       (shown as API Key Secret)
#   X_ACCESS_TOKEN    = OAuth 1.0a  "Access Token"          ← tap Generate
#   X_ACCESS_SECRET   = OAuth 1.0a  "Access Token Secret"   ← same Generate
#
# BEFORE generating the access token, open "User authentication settings" and set
# App permissions to **Read and write**. A token generated while the app was
# Read-only keeps that scope forever and every tweet fails with 403 — if you
# already generated one, change the permission and REGENERATE it.
#
# NOT USED (do not paste them here): the Bearer Token (read-only app auth) and
# the OAuth 2.0 Client ID / Client Secret. Posting on behalf of an account uses
# OAuth 1.0a, and v1 media upload accepts nothing else.
#
# Check it before you trust it:  npm run x:check       (verifies + names the handle)
#                                npm run x:check -- --tweet   (posts a real test tweet)
X_ENABLED=1                      # 0 = force every X post off, keys or not
X_LISTING_HANDLE=dexvralisting   # the account these keys belong to (no @)
X_API_KEY=
X_API_KEY_SECRET=
X_ACCESS_TOKEN=
X_ACCESS_SECRET=

# Per-source switches. THE RULE: only LISTINGS, PUMP ALERTS and BANNER ADS are
# posted to @dexvralisting. Everything else ships OFF.
#
# These defaults live in code, but an EXPLICIT value here BEATS them — a stale
# `X_RANKUP_ENABLED=1` left from an older setup silently puts rank-up tweets
# back. The boot log names every source that is live, and WARNS about any beyond
# the three allowed, so a leftover line is visible in one place instead of on
# the public timeline hours later.
X_AUTOLIST_ENABLED=1             # tweet FREE auto-listings too, not just paid ones
X_TRENDING_ENABLED=0             # OFF: Trending Token is its own product, announced in @dexvratrending
X_GAINERS_ENABLED=0              # OFF: the Top Gainers board is posted by hand from @dexvraadminbot
X_RANKUP_ENABLED=0               # OFF: fires per token on every climb — too repetitive for the feed
# Pump alerts have no switch of their own (PUMP_ENABLED covers the whole feature).
# They fire ONCE per token, ever, and are only tweeted when the token's listing
# tweet is known — the pump tweet QUOTES it, so it always reads as a follow-up.
X_POST_TIMEOUT_MS=30000          # how long a post waits for X before going out without its X link
                                 # (raise it if you use long animated banners — video is transcoded by X)

# OPTIONAL second account, only for banner ads. Leave BLANK for the normal
# one-account setup — banner ads then go out from the listing account above.
X_HANDLE=dexvraio                # the @handle named in the banner-ad copy
X_O_API_KEY=
X_O_API_KEY_SECRET=
X_O_ACCESS_TOKEN=
X_O_ACCESS_SECRET=

# ── Background services ──────────────────────────────────────────────────────
TRENDING_POST_MS=300000           # trending list refresh cadence
TRENDING_SWEEP_MS=60000           # clear expired trending slots
PUMP_CHECK_MS=180000              # pump-alert poll cadence
PUMP_ENABLED=1

# ── Rate limiting ────────────────────────────────────────────────────────────
RATE_WINDOW=3000
RATE_LIMIT=20

# ── Top Gainers banner ───────────────────────────────────────────────────────
# Nothing to set here: the channel, layout, daily time/timezone and filters all
# live in the admin panel (@dexvraadminbot → 📊 Banner Top Gainers), persisted in
# data/gainersBanner.json so a change needs no redeploy. The daily auto-post ships
# OFF. These two only exist to re-tune the pollers:
GAINERS_POLL_MS=3000              # how often the main bot picks up a queued banner
GAINERS_DAILY_CHECK_MS=30000      # how often the daily-post clock is checked

# ── Admin broadcast (compose in @dexvraadminbot → delivered by the main bot) ──
BROADCAST_RATE=20          # messages/sec (Telegram allows ~30/s to distinct users; max 28)
BROADCAST_CONCURRENCY=8    # parallel in-flight sends
BROADCAST_POLL_MS=5000     # how often the main bot picks up a queued broadcast

# ── Group Buy Bot ────────────────────────────────────────────────────────────
# A project adds @dexvrabot to their group, runs /settoken <CA>, and every
# on-chain buy of their token posts an alert there.
#
# Every alert is a REAL per-transaction alert, read from GeckoTerminal's pool
# trades feed, and carries the tx hash and buyer link that prove it. There is no
# estimated fallback: when the feed cannot be read the bot posts NOTHING and
# logs why. The buys are not lost — the cursor does not move, so they post with
# their hashes on the first poll that works, as long as that is within 30
# minutes. Verify the feed against the live API from your server:
#
#     npm run buybot:check                 # uses a known-good pool
#     npm run buybot:check -- solana <token-address>
GROUP_BUYBOT_ENABLED=1
# GeckoTerminal's free tier is ~30 requests/minute for the WHOLE process, and a
# 429 arms a two-minute process-wide cooldown during which no group gets an
# alert. If the log shows "GeckoTerminal backing off" often, that ceiling — not
# the settings below — is what is keeping groups quiet. A Pro key raises it and
# switches the client to the Pro base URL automatically.
# GECKOTERMINAL_API_KEY=
# GECKOTERMINAL_API_BASE=          # only to override the base URL by hand
#
# Every GeckoTerminal request in this process — the buy bot's trades feed AND
# the background pipelines that price tokens (pump, rank-up, auto-trend,
# gainers, trending, fulfilment) — is paced through ONE limiter, with the trades
# feed given priority because it is the only one whose lateness a group sees.
# Pacing beats backing off: one request too many costs a 120s process-wide
# cooldown in which nobody gets an alert, and delaying it costs nobody anything.
#
# The default sits under the free ceiling (25/min; 300 with a key). Raising it
# without a key just moves you back to being 429'd.
# GT_MAX_RPM=25
#
# THE ARITHMETIC THAT DECIDES WHETHER YOU NEED A KEY:
#   reads/min = pools tracked x 60000 / BUYBOT_POOL_MIN_MS
# 10 pools at 25000ms is 24/min, which fits in 25. Past that the limiter cannot
# create capacity — it only makes the shortfall graceful and prioritised instead
# of a shared blackout — and alerts will be late until you set a key or raise
# BUYBOT_POOL_MIN_MS. `npm run buybot:check` prints both numbers.
GROUP_BUYBOT_CHECK_MS=20000   # how often the scan loop wakes up
# ONE trades read per POOL per window, however fast the loop runs and however
# many groups watch the same token. GeckoTerminal's free tier is ~30 req/min for
# the WHOLE process (shared with listings, trending and pump alerts), so this —
# not the scan interval — is the real budget knob.
BUYBOT_POOL_MIN_MS=25000
# The "hype meter" row: one emoji per this many dollars, clamped. The ceiling
# matters more than the step — an uncapped row wraps to four lines on a phone
# and pushes the actual numbers off the first screen.
BUYBOT_EMOJI_STEP_USD=50
BUYBOT_EMOJI_MIN=3
BUYBOT_EMOJI_MAX=16
# Buy-size tiers. The LABELS are editable in @dexvraadminbot (🤖 Group Buy Bot →
# Buy tiers); only the thresholds live here, because a group trading in cents
# and one trading in thousands do not agree on what "whale" means.
BUYBOT_WHALE_USD=1000
BUYBOT_MEGA_USD=5000

# ── Dexvra Raid ──────────────────────────────────────────────────────────────
# A group admin runs /raid, sets goals (+15 likes, +5 replies, +10 crew), pastes
# an X post and launches. The bot posts one card and keeps it updated.
#
# THE X API IS OPTIONAL. The 🤝 Crew goal counts who shows up in the chat and
# needs no key, no plan and no quota — so a raid runs with nothing set below.
# X goals need a key; without one the raid still launches crew-only and the card
# says why.
RAID_ENABLED=1
RAID_POLL_SEC=30       # how often a live raid re-reads its post
RAID_MAX_MINUTES=60    # hard deadline; also what the boot sweep uses to free a locked group
RAID_BUMP_MINUTES=5    # how often the card may be re-posted at the bottom of the chat (floor 2)
#
# X reads cost about half a cent PER RAID, not per poll: since 2026-02-06 they
# are pay-per-use at $0.005 each, DEDUPLICATED per post per 24h UTC window. So
# polling one post 120 times over an hour bills as roughly ONE read.
#
#     one 60-minute raid  ≈ $0.005          100 raids/month ≈ $0.50
#
# The binding constraint is therefore the app-level RATE LIMIT, not money
# (~3,500 reads/15min ≈ 116 concurrent distinct posts at 30s). If you hit it,
# raise RAID_POLL_SEC — you cannot buy past a rate limit.
#
# This is an app-only Bearer Token (console.x.com → your app → Keys and tokens).
# It is a DIFFERENT credential from the four OAuth 1.0a keys above, which post;
# this one reads. Leave it blank to run crew-only raids.
# NOTE: a real token begins with a long run of A's — that is normal. The
# .env.example placeholder is detected and treated as absent, so it can never
# arm a 401 backoff that would blind every group's raid.
X_BEARER_TOKEN=
#
# Two KEYLESS sources, both OFF by default. Read the FourtisRaid-equivalent
# notes in bot/README.md before enabling either — they are not independent
# fallbacks (both are gated by X's IP reputation, so a datacenter block takes
# them out together) and neither is a sanctioned API.
RAID_FREE_METRICS=0    # likes + replies via X's embed endpoint. Cannot see reposts.
RAID_GUEST_METRICS=0   # likes + replies + REPOSTS via X's internal GraphQL + a guest token.
# Only needed if X rotates its GraphQL operation hash or feature flags — set
# them here and restart, instead of waiting for a deploy. Diagnose with:
#     npm run raid:check
X_GUEST_QUERY_ID=
X_GUEST_FEATURES=

# ── Whale wallet alerts ──────────────────────────────────────────────────────
# A DIFFERENT question from BUYBOT_WHALE_USD / BUYBOT_MEGA_USD above, which
# grade the size of the BUY. This one grades the BUYER: a wallet already holding
# this much of the token gets its own 🐋 WHALE WALLET alert, pinned in the group,
# whatever it just spent — a $200 top-up from someone sitting on $80k is news in
# a way a $200 buy from a fresh wallet is not.
#
# The holding is read ON CHAIN (the buyer's balance of that token, valued at the
# pool price). It is NOT a portfolio total — this bot has no portfolio API — so
# the card says "Holds", not "Wallet Balance". Works on Solana and every EVM
# chain here; other chains skip whale detection and alert normally.
#
# THESE THREE ARE THE SHIPPED FALLBACK, NOT THE LIVE VALUE. The bar resolves in
# three layers, most specific first:
#   1. the group's own  /setwhale 50000     (per project)
#   2. @dexvraadminbot → 🎨 Gambar Banner Channel → 🐋 Whale Alert → ⚙ Batas whale
#   3. these settings                       (what layers 1 and 2 start from)
# Layer 2 is read fresh on every check, so an operator can move the bar without
# touching this file or restarting anything. Editing here changes only what an
# instance with no admin override falls back to.
#
# Per group: /setwhale 50000 · /setwhale off · /buybot pin off
BUYBOT_WHALE_WALLET_ENABLED=1
BUYBOT_WHALE_WALLET_USD=50000
# Reading a holding costs one RPC call, so dust does not get to order one
# (results are also cached per wallet for two minutes).
BUYBOT_WHALE_CHECK_MIN_USD=100
# Whale alerts are pinned, each replacing the last. Ordinary buys never are:
# a pin per buy is not a highlight, it is a scrollbar.
BUYBOT_PIN_WHALES=1
