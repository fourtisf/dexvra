{
  "name": "discovery-listing",
  "version": "0.1.0",
  "private": true,
  "comment_engines": "Next 14 requires >=18.17. The upload routes used to need >=20 for the File global; they no longer do (src/lib/upload.ts), so 18.17 is the real floor. Production runs 18.19.",
  "engines": {
    "node": ">=18.17"
  },
  "scripts": {
    "dev": "next dev",
    "comment_build": "NEXT_PUBLIC_BUILD stamps the commit into the bundle. Without it there is no way to tell a deployed build from a stale one — a debug endpoint answered in an OLD response shape and it took a round trip to work out the server had simply not been rebuilt. `|| echo unknown` so a tarball deploy with no .git still builds.",
    "build": "NEXT_PUBLIC_BUILD=$(git rev-parse --short HEAD 2>/dev/null || echo unknown) next build",
    "start": "next start",
    "lint": "next lint",
    "gen:icons": "node scripts/gen-brand-assets.mjs",
    "test": "node --test --experimental-strip-types \"src/**/*.test.ts\"",
    "test:e2e": "node scripts/e2e-smoke.mjs",
    "comment_chart_check": "Whether an upstream ANSWERS is a property of the server's egress today, not of the code — the rule raid:check, launchpads:check and fonts:check all state. This one says it about a request shape that is a GUESS: DexScreener publishes no documented OHLCV endpoint, so a red line here is a .env fix (DS_CHART_API / DS_CHART_PATH / DS_CHART_BARS_KEY) rather than a deploy. It drives the provider's real entry points, prints the build stamp, and exits non-zero only when NEITHER source can draw.",
    "chart:check": "node scripts/chart-check.mjs",
  "comment_ds_probe": "chart:check reports that the guessed DexScreener candle shape failed and then tells the operator to read the real one out of a browser's DevTools. The box is not a browser \u2014 but it can read the browser's code. This resolves the pair through the documented api. host, greps dexscreener.com's own JS bundle for the datafeed host and path, probes each candidate with dsChart's real headers, and prints the .env lines for whatever answers with bars. A wrong guess stays a .env line, never a deploy.",
  "ds:probe": "node scripts/ds-chart-probe.mjs",
  "comment_ds_apply": "The same probe, applying what it finds: it upserts DS_CHART_API/PATH/QUERY into .env.local (after a backup) instead of printing them for a human to paste. Opt-in via --write because a probe that edits an operator's environment unasked is not a probe.",
  "ds:apply": "node scripts/ds-chart-probe.mjs --write",
    "comment_chart_preview": "The chart panel is judged by LOOKING at it — a clipped axis stamp and a smeared phone window both passed the unit tests. Needs a running server; stubs the upstreams in the browser so it works with no egress.",
    "chart:preview": "node scripts/chart-preview.mjs",
    "comment_logos_check": "Which listed tokens would draw a monogram, and WHICH of the three causes it is: the row lost its logo, the uploaded file is gone, or our own proxy refuses a working url. Drives the running server and pulls each logo through the same /api/logo the browser uses — whether a CDN answers is a property of this box's egress today. Non-zero when any row would draw a monogram.",
    "logos:check": "node scripts/logo-check.mjs"
  },
  "dependencies": {
    "mongodb": "^6.21.0",
    "next": "^14.2.35",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.19.43",
    "@types/react": "^18.3.31",
    "@types/react-dom": "^18.3.7",
    "playwright": "^1.61.1",
    "typescript": "5.5"
  }
}
